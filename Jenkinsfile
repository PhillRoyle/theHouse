pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                // sh 'make' 
                echo "********"
            }
        }
        stage('Test'){
            steps {
                // sh 'make check'
                // junit 'reports/**/*.xml' 
                echo "$$$$$$$%$$$"
            }
        }
        stage('Deploy') {
            steps {
                // sh 'make publish'
                echo "$&&&&&&&&&"
            }
        }
    }
}